pipeline {
	agent any
		stages {
			steps('Build') {
				echo 'this is the build stage'
			}
		}
		stages {
			steps('Test') {
				echo 'this is the Testing stage'
			}
		}
		stages {
			steps('Deploy') {
				echo 'this is the Deploy stage'
			}
		}
	}
}
